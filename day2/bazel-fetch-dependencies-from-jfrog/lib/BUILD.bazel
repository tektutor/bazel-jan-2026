load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "hello_lib",
    srcs = ["src/hello.cpp"],
    includes = ["inc"],
    hdrs = glob(["inc/**/*.h"]),
    strip_include_prefix = ".",
    visibility = ["//visibility:public"],  # allows other packages to depend on it
)
